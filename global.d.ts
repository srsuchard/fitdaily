// Committed counterpart to the auto-generated, gitignored `expo-env.d.ts`.
// Both just reference `expo/types`, which provides Expo's ambient declarations
// (including CSS module types like *.module.css and side-effect *.css imports).
// Committing this means `tsc` passes in CI without needing a running Expo CLI to
// generate `expo-env.d.ts` first. Duplicate reference directives are harmless.
/// <reference types="expo/types" />
